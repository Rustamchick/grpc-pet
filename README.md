# gRPC Auth Service

Тестовый backend‑проект на Go с использованием **gRPC**, реализующий сервис аутентификации и авторизации. Проект предназначен для практики сетевого взаимодействия, архитектуры backend‑сервисов и работы с современным Go‑стеком.

---

## Возможности

* gRPC API для аутентификации
* Регистрация пользователей
* Логин и выдача токенов
* Проверка валидности токена (Auth / IsAdmin)
* Работа с `context.Context`
* Чёткое разделение слоёв (handlers / service / repository)

---

## Архитектура

Проект построен с упором на читаемость и расширяемость:

```
cmd/            // точка входа (main)
internal/
  app/          // инициализация приложения
  config/       // конфигурация
  handler/      // gRPC-хендлеры
  service/      // бизнес-логика
  repository/   // работа с хранилищем
  auth/         // доменная логика аутентификации
```

Используемые подходы:

* Dependency Injection
* Clean / Layered Architecture
* Явная работа с ошибками

---

## Стек

* **Go**
* **gRPC**
* **Protocol Buffers**
* **JWT** 
* `context.Context`
* Concurrency (goroutines)

---

## Запуск проекта

### 1. Клонирование репозитория

```bash
git clone https://github.com/<username>/<repo>.git
cd <repo>
```

### 2. Генерация gRPC-кода

```bash
protoc \
  --go_out=. \
  --go-grpc_out=. \
  proto/auth.proto
```

### 3. Запуск сервера

```bash
go run ./cmd/auth
```

---

## 📡 gRPC методы (пример)

```proto
service Auth {
    rpc Register (RegisterRequest) returns (RegisterResponse);
    rpc Login (LoginRequest) returns (LoginResponse);
    rpc IsAdmin (IsAdminRequest) returns (IsAdminResponse);
}

```
